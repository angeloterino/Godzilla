@using StrawmanApp.Helpers
@{
    var _style = ViewData["_style"] == null ? "" : ViewData["_style"].ToString();
    var _has_child = ViewData["has_child"] == null ? "" : ViewData["has_child"].ToString();
    if (_has_child == "Y")
    { 
        var _style_t = "background-color: rgb(255,192,0);font-size:bold;";
        var _id = int.Parse(ViewData["id"].ToString());
        var _parent = int.Parse(ViewData["parent"].ToString());
        var _order= int.Parse(ViewData["order"].ToString());
        if(_parent == 0)
        {
            _style_t = "background-color: " + StrawmanConstants.Colors.Red + ";color:" + StrawmanConstants.Colors.White + ";";
        }
        if (_id == 0)
        {
            _style_t = "background-color: " + StrawmanConstants.Colors.Red + ";color:" + StrawmanConstants.Colors.White + ";";
            if (_id == 0) { _style_t = "background-color: " + StrawmanConstants.Colors.Green + ";color:" + StrawmanConstants.Colors.Black + ";"; }
        }
        _style += _style_t;
    }
}
style="@_style"