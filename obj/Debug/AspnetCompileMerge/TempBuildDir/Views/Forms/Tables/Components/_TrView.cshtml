@using StrawmanApp.Classes
@if (ViewData[HtmlElements.TABLE_ROWS] != null){
    List<ViewDataDictionary> rows = (List<ViewDataDictionary>)ViewData[HtmlElements.TABLE_ROWS];
    foreach(ViewDataDictionary row in rows){
        <tr @row[HtmlElements.DATA_ATTRIBUTES] class="@Html.Raw(row[HtmlElements.CLASSES])">
            @foreach (ViewDataDictionary cell in (List<ViewDataDictionary>)row[HtmlElements.CONTENT])
            {
                Html.RenderPartial(cell[HtmlElements.PARTIAL_VIEW].ToString(), new ViewDataDictionary { { HtmlElements.CONTENT, cell[HtmlElements.CONTENT] } });
            }
        </tr>
    }
}